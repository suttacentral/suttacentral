# Scripts Directory

Project scripts reside in this directory. With the removal of `wait_for_arango.py` we have none here at the moment. 

The `Dockerfile-flask` build will fail if the `scripts/` directory is empty.