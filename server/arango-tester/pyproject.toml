[project]
name = "arango-tester"
version = "0.1.0"
description = "Unit test for sc-arangodb container health check"
readme = "README.md"
requires-python = ">=3.11.3"
dependencies = [
    "pytest>=9.0.2",
    "python-arango>=8.3.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]